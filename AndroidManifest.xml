<?xml version="1.0" encoding="utf-8"?>
<manifest 
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.MyMascots.namespace"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".MainMisMascotas" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name="Mascotas" 
            android:label="@string/_misMascotas"/>
        <activity 
            android:name="Citas" 
            android:label="@string/_proximasCitas"/>
        <activity 
            android:name="Solicitar" 
            android:label="@string/_solicitarCita"/>
        <activity android:name="EditMascota" android:label="Mi mascota"></activity>
        <activity android:name="EditCita" android:label="Editar cita"></activity>
        <activity android:label="@string/_avis" android:theme="@android:style/Theme.Dialog" android:name="Mensaje"></activity>
    </application>

</manifest>